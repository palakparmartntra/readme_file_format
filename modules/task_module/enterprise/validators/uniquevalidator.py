class UniqueValidator:
    """[summary]"""

    def validate(self, data):
        """[summary]

        Args:
            data ([type]): [description]

        Returns:
            [type]: [description]
        """
        if data:
            return False
        else:
            return True
